# Anti-UAV 환경에서 UNet 세그멘테이션과 칼만 필터를 융합한 드론 위치 추적 시스템

## 1. 서론

### 1.1 연구 배경

소형 무인 항공기(UAV)의 비인가 침입은 공항, 군사시설, 주요 인프라에 보안 위협이 되고 있다. Anti-UAV 시스템에서는 드론을 실시간으로 탐지하고 추적하는 기술이 핵심이다.

기존 방식은 바운딩 박스 탐지 + 등속도 추적기를 사용하며, (1) 픽셀 수준의 형태 정보 미활용, (2) 프레임 간 노이즈에 의한 추적 불안정(jitter), (3) 비선형 궤적 예측 불가의 한계를 갖는다.

### 1.2 연구 목적

UNet 세그멘테이션과 칼만 필터를 융합하여 안정적 추적을 달성한다:
1. UNet으로 드론 마스크 생성, 중심점(centroid) 추출
2. 칼만 필터로 시간축 평활화 적용
3. 등속도(CV), 등가속도(CA), 비선형(EKF-CTRV) 모션 모델 비교

---

## 2. 제안 방법

### 2.1 전체 아키텍처

```
입력 프레임 (480×480×3)
    → [UNet] → 객체 마스크 (480×480×1)
    → [Centroid 추출] → 측정값 z=[x,y]
    → [Kalman Filter Predict/Update] → 평활화된 위치
```

### 2.2 UNet 세그멘테이션

| 파라미터 | 값 |
|----------|-----|
| 구조 | VanillaUNet (Encoder-Decoder + Skip Connection) |
| 입력/출력 | 3ch → 1ch (이진 분할) |
| 시작 채널 | 32 |
| 깊이 | 4 |
| 손실 함수 | BCEWithLogitsLoss |
| Optimizer | AdamW (lr=1e-4) |

### 2.3 모션 모델

**Constant Velocity (CV) — 선형 칼만 필터**
- 상태: [x, y, vx, vy] (4D)
- 가정: 속도 일정

**Constant Acceleration (CA)**
- 상태: [x, y, vx, vy, ax, ay] (6D)
- 가정: 가속도 일정, 여전히 선형

**CTRV — Extended Kalman Filter**
- 상태: [x, y, v, θ, ω] (5D)
- 비선형 상태 전이, 야코비안 기반

### 2.4 시퀀스 전환 처리

다중 시퀀스에서 중심점이 100px 이상 점프하면 칼만 필터를 리셋하여 불연속 대응.

---

## 3. 실험

### 3.1 환경

| 항목 | 내용 |
|------|------|
| 데이터셋 | ANTI-UAV RGBT (적외선/가시광) |
| 학습/테스트 | 4 시퀀스 학습, 1 시퀀스 테스트 (1,708 프레임) |
| 이미지 크기 | 480 × 480 |
| 학습 | 50 epochs, AdamW, BCEWithLogitsLoss |

### 3.2 평가 지표

| 지표 | 설명 | 방향 |
|------|------|------|
| mIoU | 마스크 교집합/합집합 | ↑ |
| Dice | F1 기반 마스크 정확도 | ↑ |
| CLE | 예측-GT 중심점 거리 (px) | ↓ |
| Jitter | 프레임 간 가속도 변동 (px/frame²) | ↓ |
| Smoothness | Raw jitter / Filtered jitter | ↑ |

---

## 4. 결과

### 4.1 세그멘테이션 성능

| 모델 | mIoU | Dice |
|------|------|------|
| VanillaUNet (ch=32, depth=4) | 0.866 | 0.926 |

### 4.2 필터 비교 (Q=0.3, R=0.5)

| 방법 | CLE (px) ↓ | Jitter ↓ | Jitter 감소 | Smoothness ↑ |
|------|-----------|----------|-------------|--------------|
| Baseline (UNet only) | **2.88** | 3.63 | — | 1.00 |
| Linear KF (CV) | 3.22 | **2.75** | **-24.0%** | **1.32** |
| CA (등가속도) | 3.13 | 3.08 | -15.0% | 1.18 |
| EKF (CTRV) | 3.27 | 3.35 | -7.5% | 1.08 |

### 4.3 Q 파라미터 민감도 (Linear KF, Sweep)

| Q | CLE (px) | ΔCLE | Jitter | Jitter 감소 | Smoothness |
|---|----------|------|--------|-------------|------------|
| 0.01 | 5.52 | +2.64 | 1.74 | 52% | 2.09 |
| 0.05 | 4.08 | +1.21 | 2.19 | 40% | 1.66 |
| 0.1 | 3.67 | +0.79 | 2.40 | 34% | 1.51 |
| **0.3** | **3.22** | **+0.34** | **2.75** | **24%** | **1.32** |
| 0.5 | 3.09 | +0.21 | 2.92 | 19% | 1.24 |
| 1.0 | 2.97 | +0.09 | 3.13 | 14% | 1.16 |

---

## 5. 분석

### 5.1 선형 칼만 필터가 최적인 이유

- Jitter 24% 감소, CLE 증가는 0.34px (480px 대비 0.07%)
- 4상태 + 2관측의 균형 → 안정적 추정
- 등속 위주의 UAV 운동에 모델 가정이 적합

### 5.2 EKF(CTRV)의 한계

- 5상태를 2관측으로 추정 → 구조적 과적합
- heading, yaw_rate 추정 불안정 (직선 운동에서)
- 급선회 데이터가 있으면 효과 발현 예상

### 5.3 Trade-off

Q↓ → 평활화↑ + CLE↑ (lag). Q↑ → CLE↓ + 평활화↓. 응용에 따라 선택:
- 실시간 추적 시스템: 안정성 우선 → Q 작게
- 정밀 위치 추정: CLE 우선 → Q 크게

---

## 6. 결론

1. UNet이 mIoU 0.866으로 충분한 세그멘테이션 정확도 달성
2. **선형 칼만 필터(CV)**가 종합 최적 — Jitter 24%↓, CLE 손실 0.34px
3. CA는 CLE에서 소폭 우위, EKF는 본 데이터에서 비효과적
4. 칼만 필터의 핵심 기여: **위치 정확도보다 추적 궤적의 시간적 안정성 확보**

---

## 7. 향후 연구

- 급선회/고기동 시퀀스에서 EKF 재평가
- 가림(occlusion) 시 칼만 필터 예측 기반 추적 유지
- IMM 필터로 모션 모드 자동 전환
- 경량 UNet에서 칼만 필터 효과 비교 (세그멘테이션 정확도 낮을 때)
